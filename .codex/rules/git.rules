prefix_rule(
    pattern = ["git", "commit"],
    decision = "forbidden",
    justification = "Codex must not create commits; leave committing to the human operator",
    match = [
        "git commit",
        "git commit -m 'message'",
        "git commit --amend",
    ],
)
